# 운영비 산정

작성 시점: 2026-08-01 · 대상: fly.io `open-discord-casino` (리전 `nrt`, shared-cpu-1x 256MB, 볼륨 1GB)

모든 숫자는 fly.io 공식 요금표 인용값과 로컬 실측값만 사용했다.
근거가 없는 항목은 **확인 불가**로 표시했다.

---

## 1. 단가 (fly.io 공식 요금표 인용)

| 항목 | 단가 |
| --- | --- |
| shared-cpu-1x 256MB | `$0.00000078`/초 = **$2.02/월** (상시 가동, 720시간 기준) |
| 볼륨 | **$0.15**/GB/월 (프로비저닝한 용량 기준 — 안 써도 부과) |
| 이그레스 (아시아·태평양) | **$0.04**/GB |
| 중지된 머신 rootfs | **$0.15**/GB/30일 |
| 공유 IPv4 + Anycast IPv6 | **무료** |
| 단일 호스트명 인증서 | 조직당 첫 10개 **무료** |
| 디스코드 앱·봇 | **무료** |

무료 티어는 없다. 현재 Pay-as-You-Go 요금제에 컴퓨트/스토리지 무료 할당량은 제공되지 않는다.

---

## 2. 전송량 실측 (gzip 적용 후, 응답 헤더 포함)

측정 방법: 로컬 서버에 원시 `node:http`로 요청해 선로 위 바이트를 셌다.
(`fetch`는 accept-encoding을 임의로 붙이고 응답을 자동 해제해 실제 전송량을 볼 수 없다.)

| 게임 | 폴링 1회 | 1초 폴링 시 시간당/인 |
| --- | --- | --- |
| 포커 플립 | 972 B | **3.34 MB** |
| 사다리 | 436 B | **1.50 MB** |
| 그래프 | 443 B | **1.52 MB** |
| 지뢰찾기 | 폴링 없음 (조작할 때만 요청) | — |

3개 게임 혼합 평균 **약 2.1 MB/인시**.

첫 접속 시 1회 전송량:

| 대상 | 크기 |
| --- | --- |
| 로비 | 15.2 KB |
| 포커 플립 페이지 | 24.9 KB |
| 사다리 / 그래프 / 지뢰찾기 페이지 | 21.8 / 21.9 / 17.9 KB |
| 포커 효과음 5종 | 292 KB (7일 캐시) |
| 그래프·사다리 팡파레 | 314 KB (7일 캐시) |
| 지뢰찾기 효과음 3종 | 78 KB (7일 캐시) |
| 카드 SVG | 549 B/장 (7일 캐시) |

폴링은 **탭이 숨겨지거나 3분간 조작이 없으면 멈춘다.** 즉 창만 열어둔 유저는 요금을 발생시키지 않는다.

### gzip 적용 효과

배포 준비 중 응답 압축을 넣었다. 적용 전후:

| 대상 | 이전 | 이후 |
| --- | --- | --- |
| 포커 페이지 | 80,793 B | 24,642 B (30%) |
| 포커 폴링 (본문) | 2,573 B | 561 B (22%) |
| 사다리 폴링 (본문) | 844 B | 213 B (25%) |
| 효과음 `card-shuffle.wav` | 353,530 B | 110,694 B (31%) |

폴링 트래픽이 **약 1/3로 줄었다.** 이그레스는 상시 반복 비용이라 이 차이가 그대로 요금에 남는다.
(mp3처럼 압축 이득이 없는 파일은 원본으로 보낸다.)

---

## 3. 시나리오별 월 운영비

머신은 scale-to-zero다. **누군가 플레이 중인 시간만** 컴퓨트 요금이 붙는다.

> **확인 불가** — 마지막 요청 후 fly가 머신을 재우기까지의 정확한 유휴 시간.
> 공식 문서는 "stop 루프가 몇 분마다 돈다", "앱이 몇 분간 유휴일 때"라고만 적고
> 구체적 초·분을 명시하지 않는다. 아래는 **세션당 5분의 꼬리**를 가정했다.

### A. 소규모 — 하루 2시간 활성, 평균 동시 2명

| 항목 | 계산 | 금액 |
| --- | --- | --- |
| 컴퓨트 | (2h + 20분 꼬리) × 30일 = 70h × $0.002808 | $0.20 |
| 이그레스 | 120 인시 × 2.1MB + 자산 6MB ≈ 0.26 GB | $0.01 |
| 볼륨 1GB | | $0.15 |
| 중지 머신 rootfs | 이미지 크기 확인 불가 (아래 참고) | ≤$0.05 |
| **합계** | | **약 $0.41 / 월** |

### B. 보통 — 하루 6시간 활성, 평균 동시 4명

| 항목 | 계산 | 금액 |
| --- | --- | --- |
| 컴퓨트 | (6h + 30분 꼬리) × 30일 = 195h × $0.002808 | $0.55 |
| 이그레스 | 720 인시 × 2.1MB + 자산 12MB ≈ 1.52 GB | $0.06 |
| 볼륨 1GB | | $0.15 |
| 중지 머신 rootfs | | ≤$0.05 |
| **합계** | | **약 $0.81 / 월** |

### C. 상시 — 사실상 24시간 누군가 접속 (머신이 안 잠듦), 평균 동시 3명

| 항목 | 계산 | 금액 |
| --- | --- | --- |
| 컴퓨트 | 720h 전부 = 상한 | $2.02 |
| 이그레스 | 2,160 인시 × 2.1MB ≈ 4.54 GB | $0.18 |
| 볼륨 1GB | | $0.15 |
| 중지 머신 rootfs | 중지되지 않으므로 없음 | $0 |
| **합계** | | **약 $2.35 / 월** |

### 결론

**월 $0.4 ~ $2.4** (환율 1,400원/$ 가정 시 약 **600원 ~ 3,400원**).

컴퓨트는 상시 가동해도 $2.02가 상한이므로, 이용자가 늘어도 요금이 폭증하는 구조가 아니다.
이그레스가 컴퓨트 상한($2.02)만큼 나오려면 월 50GB를 써야 하는데,
이는 **혼합 평균 기준 약 24,000 인시**(= 매일 24시간 동안 평균 33명이 플레이)에 해당한다.

---

## 4. 확인 불가 항목

| 항목 | 이유 | 요금 영향 |
| --- | --- | --- |
| 빌드된 이미지 크기 | 로컬 Docker 데몬이 꺼져 있어 실측 불가. 구성 요소는 실측했다 — `node:22-slim` 76MB(압축, Docker Hub API), 프로덕션 `node_modules` 29.2MB, `src` 313KB, `public` 1,328KB | rootfs 요금. 이미지가 300MB여도 월 $0.045 |
| fly의 정확한 유휴→중지 대기 시간 | 공식 문서에 수치 없음 | 시나리오 A·B의 컴퓨트. 꼬리가 5분→10분이 되면 A는 $0.20→$0.28 |
| 중지 머신을 깨우는 시간 | 배포 전에는 측정 불가 | 요금이 아니라 **기능** 문제. 디스코드 인터랙션 3초 제한을 넘기면 출석체크가 실패한다. `DEPLOY.md` 8-3 참고 |
| suspend 모드의 과금과 볼륨 호환성 | 공식 문서·커뮤니티 글 모두 언급 없음 | suspend로 전환할 경우 미지 |
| 헬스체크가 머신을 깨워두는지 | 공식 문서에 명시 없음 | 깨워둔다면 컴퓨트가 $2.02로 고정된다. 그래서 `fly.toml`에 헬스체크를 넣지 않았다 |

---

## 5. 참고 — 기존 앱과의 비교

같은 계정의 `discord-lol-bot`은 동일 사양(shared-cpu-1x 256MB, 볼륨 1GB, `nrt`)이지만
`flyctl machine list` 확인 결과 **`started` 상태로 상시 가동 중**이다.
디스코드 게이트웨이(웹소켓)를 상시 연결하는 구조라 잠들 수 없다.

카지노는 게이트웨이 없이 HTTP 인터랙션 웹훅만 쓰고 서버 타이머도 없으므로
**아무도 접속하지 않으면 완전히 잠든다.** 이것이 시나리오 A·B가 성립하는 근거다.

---

## 6. 요금을 늘리는 선택지 (필요할 때만)

| 변경 | 추가 비용 | 언제 |
| --- | --- | --- |
| `min_machines_running = 1` | 컴퓨트가 $2.02/월로 고정 | 디스코드 3초 제한을 못 맞출 때 |
| 볼륨 증설 (1GB → 3GB) | +$0.30/월 | 현재 DB 488KB, 보존 정책이 있어 당분간 불필요 |
| 전용 IPv4 | +$2.00/월 | 커스텀 도메인에 A 레코드가 필요할 때. `*.fly.dev`만 쓰면 불필요 |
| 메모리 512MB로 증설 | 컴퓨트 단가 약 2배 | 현재 RSS 104MB로 여유 있음 |
